specVersion: "v1alpha"
serviceId: "app"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "app"
      cloudSql:
        instanceId: "movies-app"
connectorDirs: ["./connector"]
